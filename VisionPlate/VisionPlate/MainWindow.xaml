<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VisionPlate"
		xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
		x:Class="VisionPlate.MainWindow"
        Title="Vision Plate" Height="400" Width="600" AllowsTransparency="True" WindowStyle="None" Background="#66AADDFF" Topmost="True" ResizeMode="CanResizeWithGrip">
	<Window.DataContext>
		<local:AppModel/>
	</Window.DataContext>
	<i:Interaction.Triggers>
		<i:EventTrigger EventName="Closing">
			<ei:ChangePropertyAction TargetObject="{Binding StopVideo, Mode=OneWay}" PropertyName="Value"/>
		</i:EventTrigger>
	</i:Interaction.Triggers>
	<Grid>
		<Image Source="{Binding VideoBitmap.Value, Mode=OneWay}" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.6"/>
	</Grid>
</Window>
